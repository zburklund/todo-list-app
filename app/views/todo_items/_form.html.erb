<%= form_for([@todo_list, @todo_list.todo_items.build]) do |f| %>
	<%= f.text_field :position, placeholder: "##" %>
	<%= f.text_field :title, placeholder: "New Item" %>
	<%= f.text_field :is_done, placeholder: "true" %>
	<%= f.submit %>
<% end %>